<% provide(:title, 'All users') %>

<h1 class="center">All users</h1>

<%= will_paginate @users, class: "pagination" %>

<ul class="users">
	<% @users.each do |user| %>
		<%= render user %>
	<% end %>
</ul>

<%= will_paginate @users, class: "pagination" %>